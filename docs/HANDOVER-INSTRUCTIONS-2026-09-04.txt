================================================================================
EZER HRMS — DATABASE HANDOVER
Prepared 4 September 2026 · for Nayan Ahuja
Branch TusharPanwar @ 154928f
================================================================================

WHAT THIS IS

The same eight migrations as the 3 September handover. Nothing new has been
added to the pile — everything I have built since then runs on tables you
already have.

What HAS changed is that I probed the live database this time instead of
working from the previous list, so section 2 is measured rather than
remembered. There is also one new question for you, in section 5.

Nothing here has been run against the database. Nobody but you applies schema
changes to this project.


--------------------------------------------------------------------------------
1. RUN THESE, IN THIS ORDER
--------------------------------------------------------------------------------

    migrations/074_pms_not_found_guards.sql
    migrations/075_ess_notification_catalogue.sql
    migrations/079_group_profile.sql
    migrations/082_access_foundation.sql        <-- 084 will not work without it
    migrations/084_wall_of_fame.sql
    migrations/085_wall_of_fame_seed.sql
    migrations/086_shoutouts_and_feed.sql
    migrations/087_social_and_inbox.sql

Order matters in exactly two places:

  * 082 BEFORE 084. 084 calls can() at five sites and 082 creates it. Run 084
    first and every access check in the Wall of Fame fails closed.

    082 also creates explain_access(). Nothing in 084-087 calls that one —
    wof_explain_access(), which 084 defines itself, is the one in use.
    explain_access() is there because the bundle's brief named it and a future
    route may expect it. Said plainly so you know which of the two is
    load-bearing.

  * 084 BEFORE 085, 086 and 087. Those three ALTER tables that 084 creates and
    seed a catalogue 084 defines.

Every file is safe to run twice: CREATE TABLE IF NOT EXISTS, ADD COLUMN IF NOT
EXISTS, CREATE OR REPLACE FUNCTION, ON CONFLICT DO NOTHING. If a run
half-fails, fix the cause and run the whole file again.

There is no 083. The number is deliberately skipped.


--------------------------------------------------------------------------------
2. WHAT IS ALREADY IN THE DATABASE — measured 4 September
--------------------------------------------------------------------------------

I probed rather than assumed. These came back present, with the evidence I
used:

    066_pms_module          all 15 pms_* tables readable
                            3 policies · 12 periods · 395 enrolment rows
    067_pms_org_chain       departments.hod_employee_id selectable
    068_pms_setup_data      pms_rating_scale has 15 rows
    076_pms_persist_scores  pms_overall_rating.self_score selectable
    077_company_profile     company_contacts readable
    078_company_documents   company_documents readable
    080_ess_inbox           tables exist -- BUT SEE SECTION 5
    081_registration_docs   registrations.document_path selectable

And these came back absent, each with PGRST205 on a table the file creates:

    074 (see below) · 075 · 079 · 082 · 084 · 085 · 086 · 087

Copies of the applied files are in reference/ so you can diff them against
what is live. They are idempotent; re-running is harmless.

A CAVEAT I OWE YOU. My first probe reported six of those eight as APPLIED. It
used head:true together with .limit(0), and that combination swallowed the
error and returned a clean empty result. The second probe -- a real SELECT
alongside the count -- returned PGRST205 on all six. The list above comes from
the second method, cross-checked two ways. I am telling you because if I had
not re-run it this file would have confidently told you the opposite.

ONE THING I COULD NOT DETERMINE, and I am not going to guess:

  Is 074 applied? It creates three functions and no tables. PostgREST answers
  PGRST202 both for "no such function" and for "function exists, wrong
  arguments", so a probe cannot separate them. 074 is CREATE OR REPLACE
  throughout, so run it regardless -- that costs nothing and settles it.


--------------------------------------------------------------------------------
3. WHAT EACH FILE SWITCHES ON
--------------------------------------------------------------------------------

  074  Guard functions for the PMS: pms_open_period, pms_validate_kras,
       pms_lock_kras. pms_lock_kras is the one that enforces the rule the
       whole appraisal hangs on -- weightage cannot lock until BOTH the
       employee and the manager have acknowledged the KRA one-to-one.

  075  The ESS notification catalogue. Without it the portal has no titles or
       templates to render notifications from.

  079  group_profile. The group-level company record above the individual
       companies.

  082  can(), explain_access() and access_permission_map. The access
       foundation the Wall of Fame is built on.

  084  Wall of Fame: badge master, employee badges, recognition values.

  085  Seeds 084's catalogues. Six INSERT blocks, all ON CONFLICT DO NOTHING.

  086  Shoutouts and the feed.

  087  Comments, mentions and the inbox wiring.


--------------------------------------------------------------------------------
4. AFTER YOU RUN THEM — how to tell it worked
--------------------------------------------------------------------------------

Open the app and check three screens rather than trusting the SQL to have
meant what it said:

  ESS portal -> Wall of Fame     should render badges and the feed instead of
                                 a "not switched on yet" panel

  ESS portal -> Inbox            see section 5 first; this one has a separate
                                 problem that these eight files do not fix

  Performance (main menu)        already works today. If the Policies tab is
                                 empty that means nothing is CONFIGURED, not
                                 that anything is missing -- 066 is applied and
                                 the screen says so correctly now.

If a Wall screen still shows the pending state after 084-087, the usual cause
is PostgREST's schema cache rather than the migration. It refreshes on its own
shortly, or you can force it from the Supabase dashboard.


--------------------------------------------------------------------------------
5. A DECISION I NEED FROM YOU — the ESS inbox
--------------------------------------------------------------------------------

inbox_conversations and inbox_messages EXIST, but reading them as anon gives:

    42501 -- permission denied for table inbox_conversations
    hint: Grant the required privileges to the current role with:
          GRANT SELECT ON public.inbox_conversations TO anon;

I read 080_ess_inbox.sql: it issues no GRANT and creates no RLS policy. So the
tables were created without either and PostgREST cannot reach them. Every
other table in the project is readable, so something grants by default and
these two missed it -- I cannot tell from outside which.

I have NOT written a migration for this, on purpose. The house rule is that a
new table's RLS policy is your decision rather than something copied from an
older file, and an inbox is exactly the wrong place for an allow-all default:
these rows are private messages between named employees.

Three options, worst to best as I see it:

  1. GRANT SELECT ... TO anon, no RLS.
     Every employee can read every conversation in the company. Please do not.

  2. Grants plus RLS scoped to participants -- a row is visible when the
     reader is its sender or its recipient. This is what the feature assumes,
     and what I would write if you tell me to.

  3. All inbox reads through a SECURITY DEFINER function, nothing granted
     directly. Tightest, and the most work.

Tell me which and I will write the file. Until then the ESS inbox stays dark,
which is the correct failure -- it fails closed, not open.


--------------------------------------------------------------------------------
6. DO NOT RUN THESE
--------------------------------------------------------------------------------

  055, 056, 057   An old, already-rolled-back RMS attempt occupies those
                  numbers on this branch. The PMS module was renumbered
                  055->066, 056->067, 057->068. Running the 055-057 files
                  would apply a superseded schema on top of a live one.

  079_pms_module_v2.sql from the PMS spec folder
                  It carries the same 15 tables as 066, which is already
                  applied. On this branch 079 is group_profile, an unrelated
                  file. Adding the spec's copy as 079 would collide with it.


--------------------------------------------------------------------------------
7. THE RULE THAT MUST NOT MOVE
--------------------------------------------------------------------------------

pms_policies.payout_linkage_enabled is pinned false by a CHECK constraint:

    payout_linkage_enabled boolean NOT NULL DEFAULT false
                           CHECK (payout_linkage_enabled = false)

The PMS is developmental. The rating has no link to increment, variable pay,
bonus, or CTC, and additional benefits are recognition only -- certificate,
nomination, award, no cash component. Enabling linkage would take a migration
that drops the constraint, which is a product decision rather than a config
one. Leave the constraint where it is.


--------------------------------------------------------------------------------
8. IF SOMETHING GOES WRONG
--------------------------------------------------------------------------------

Every file is idempotent, so the first move is almost always to fix the cause
and run the whole file again rather than to unpick a partial run.

  PGRST205 in the app after a migration ran cleanly
      PostgREST's schema cache has not caught up. Wait, or reload it from the
      dashboard.

  42501 permission denied
      A grant is missing, not a policy. See section 5 -- the error text names
      the exact statement that fixes it.

  42P01 relation does not exist, mid-file
      Run order. Check 082-before-084 and 084-before-085/086/087.

  Anything you would rather not decide alone
      Send it to me with the error text and I will look. Do not work around a
      constraint to make a file run -- the constraints are load-bearing, and
      one of them (section 7) is a product commitment.

================================================================================
MANIFEST.txt lists a sha256 for every file, so a truncated or altered transfer
is detectable. Check it before you run anything.
================================================================================
